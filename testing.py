from src.items.warframes import WARFRAMES_DICT
from math import floor

if __name__ == "__main__":
    # print(len(WARFRAMES_DICT))
    # print(floor(len(WARFRAMES_DICT) / 4))
    # print(len(WARFRAMES_DICT) // 4)
    # print(len(WARFRAMES_DICT) % 4)

    # number_of_items = len(WARFRAMES_DICT)
    # columns = 4
    # number_of_rows = (
    #     (number_of_items // columns) + 1 if (number_of_items % columns) > 0 else 0
    # )

    # print(number_of_rows)

    print(len("Ash                    "))
    print(len("# Ash Prime              "))
    print(len("# Atlas                  "))
    print(len("# Atlas Prime            "))
